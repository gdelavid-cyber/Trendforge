# S1 Implementation Plan — Skeleton (Nav IA · Brand · Retell)

Spec: `docs/superpowers/specs/2026-08-23-trendly-companion-platform-design.md`

## Constraints
- No route moves · no API/schema changes · pure UI/docs.
- GitHub repo URL stays `.../gdelavid-cyber/Trendforge` (deploy-template links
  depend on it) — only display copy changes.
- Each task lands as its own commit; `tsc --noEmit` + Vitest green before each.

## T1 — Header nav IA (`components/header.tsx`)
Desktop signed-in nav becomes: Dashboard · Earn ▾ · My Companion ▾ · The World · Build ▾ · Community · Market ▾.
Groups:
- Earn ▾: Weekly Tasks `/tasks` · Trends Radar `/trends` · Success Stories `/stories`
- My Companion ▾: The Forge `/avatar-studio` · Approval Inbox `/approvals` *(hidden until S2)*
- Build ▾: Agent Studio `/builder` · My Agents `/agents` · Workflows `/workflows`
- Market ▾: Marketplace `/marketplace` · Referrals `/referrals` · Pricing `/pricing`
Admin cluster (admins only) collapses Brain/Health into one Admin ▾ menu
(Brain / Health / Swarm Console `/admin/swarm` if a page exists — API-only today,
so Brain/Health only).
Mobile menu mirrors groups as section headers + items. Anonymous nav: Earn ▾,
The World, Marketplace, Pricing + auth CTAs.
Verify: nav renders all routes; active-state still works on group children.

## T2 — Footer (`components/footer.tsx`, new)
Minimal footer mounted in `app/layout.tsx`: columns Product (Pricing, Stories)
· Company (Legal `/legal`, Compliance `/compliance`, Status `/status`,
Enterprise `/enterprise`) · social placeholders. Dark/cyan styling consistent
with header. Verify: renders on `/` and `/legal`.

## T3 — Demotions
`app/dev/stage3d/page.tsx`: add `robots: { index: false }` to metadata.
Verify: grep that nothing else links `/dev/*`.

## T4 — Trendly brand sweep
Code (display copy only):
- `app/arena/page.tsx:11` title → `The World // Trendly`
- `app/dev/stage3d/page.tsx:8` title → `Dev QA // Stage3D // Trendly`
- `scripts/seed.ts:20` log → `Seeding Trendly database...`; line 43 name →
  `Trendly Admin`; re-run seeder so the live row renames.
Docs:
- `README.md`, `.project_instructions.md`, `CONTRIBUTING.md`: TrendForge→Trendly;
  rewrite `.project_instructions.md` Design System to actual reality
  (#06060E bg, #00F0FF primary, Orbitron display, mono UI font).
Leave: git-history references, GitHub repo URL strings, spec's historical note.
Verify: `grep -r TrendForge` returns only intentional mentions.

## T5 — Landing retell (light)
Adjust hero headline/subhead/CTA copy to companion-economy framing
("Forge your companion. Pick a trending task. One click.") without moving
sections; deeper redesign deferred. Verify: hero renders, CTA targets signup.

## T6 — Dashboard retell (light)
**DEVIATION — SKIPPED:** dashboard already opens with the 3D companion
debrief banner and prominent tasks links; no quick-actions block exists to
reorder. Building one is deferred to S4 (Companion identity) rather than
inventing throwaway UI now.

## Ship checklist
1. `npx tsc --noEmit` ✅ · 2. `npm test` ✅ · 3. commits per task ·
4. push → Vercel probes: `/` `/arena` `/tasks` `/approvals`(404 ok) footer present,
   header groups render signed-out.
